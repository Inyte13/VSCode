var name="Luis" // Forma antigua de asignar una variable (No recomendado)
let age=19 // Solo accesibles dentro de un bloque
const pi=3.1416 // Constante, no se puede cambiar
let nothing // Variable no asignada es "undefined"
let nulo=null // Intencionadamente tiene el valor de "null"